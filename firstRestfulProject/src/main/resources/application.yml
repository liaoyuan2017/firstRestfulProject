spring:
  datasource:
    platform: mysql
    url: jdbc:mysql://localhost:3306/canna?serverTimezone=UTC&useSSL=false
    username: root
    password: canna
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
    ddl-auto: update
    show-sql: true
